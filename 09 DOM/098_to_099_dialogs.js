// Dialogs //
// => pop ups the use will see
// => modals are almost always a better choice

alert('this is an alert');
// => method on the window object
// => mostly used for debugging
// => argument is the text you want displayed

confirm('are you sure?');
// user can click 'Ok' or 'Cancel'
// can also be logged/ stored returning a boolean

prompt('what is your name?')
// user input can logged/stored
